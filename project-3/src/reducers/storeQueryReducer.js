/* import { STORE_QUERY } from '../actions/index'


const initialState = {
    query: null
}

const storeQueryReducers = (state = initialState, action) => {
    switch(action.type) {
        case STORE_QUERY:
            return {
                ...state,
                query: action.query
            }
        default:
            return {
                ...state
            }
    }
}

export default storeQueryReducers;  */